alleged_sayyid = {
	category = fame
	shown_in_ruler_designer = no
	
	islam_religion_opinion = 5
	
	name = {
		first_valid = {
			triggered_desc = {
				trigger = { NOT = { exists = this } }
				desc = trait_alleged_sayyid
			}
			triggered_desc = {
				trigger = { is_female = yes }
				desc = trait_alleged_sayyida
			}
			desc = trait_alleged_sayyid
		}
	}

	desc = {
		first_valid = {
			triggered_desc = {
				trigger = { NOT = { exists = this } }
				desc = trait_alleged_sayyid_desc
			}
			desc = trait_alleged_sayyid_character_desc
		}
	}

	compatibility = {
		sayyid = -5
	}
}